## 区块类型的定制

动态页面文件中，通过 `page.sections` 定义要展示的区块列表，`page.sections` 的字段类型是 `Record<string, Section>`，其中 `Section` 类型定义为：

```
export interface Section {
  id?: string;
  block?: string;
  label?: string;
  sr_only_title?: string;
  title?: string;
  description?: string;
  tip?: string;
  buttons?: Button[];
  icon?: string | ReactNode;
  image?: Image;
  image_invert?: Image;
  items?: SectionItem[];
  image_position?: 'left' | 'right' | 'top' | 'bottom' | 'center';
  text_align?: 'left' | 'center' | 'right';
  className?: string;
  component?: ReactNode;
  [key: string]: any;
}
```

我们往动态页面文件中 `page.sections` 字段添加的区块，要按照 `Section` 类型定义的字段添加内容。根据区块的 `key`，或者区块内容的 `block` 定位要展示的区块代码。

比如，我们希望展示一个特性列表区块，可以在 `page.sections` 中添加：

```
{
  "page": {
    "sections": {
      "features": {
        "block": "features-list",
        "title": "Features List",
        "description": "Features List",
        "items": [
          {
            "title": "Feature 1 Title",
            "description": "Feature 1 Description",
            "icon": "Sparkles"
          }
        ]
      }
    }
  }
}
```

按照上面的定义，在动态页面渲染时，默认会加载 `src/themes/default/blocks/features-list.tsx` 文件，渲染特性列表区块。

如果没有指定 `block` 字段，默认会加载 `src/themes/default/blocks/features.tsx` 文件，渲染特性列表区块。`features` 在这里是区块的 `key`。

`features`，`features-list` 是 ShipAny 在 default 主题内置的区块，所以配置了就能直接展示。


SHIPANY 在 default 主题内置了以下区块：
- `header`: 顶部导航区块 ([查看配置示例](./区块配置示例/header.md))

- `hero`：主区块 ([查看配置示例](./区块配置示例/hero.md))


- `logos`：Logo 列表区块 ([查看配置示例](./区块配置示例/logos.md))

- `features`：功能特点区块 ([查看配置示例](./区块配置示例/features/features.md))

- `features-list`：特性列表区块 ([查看配置示例](./区块配置示例/features/features-list.md))

- `features-accordion`：特性折叠区块 ([查看配置示例](./区块配置示例/features/features-accordion.md))

- `features-flow`：特性流程区块 ([查看配置示例](./区块配置示例/features/features-flow.md))

- `features-media`：特性媒体区块 ([查看配置示例](./区块配置示例/features/features-media.md))

- `features-step`：特性步骤区块 ([查看配置示例](./区块配置示例/features/features-step.md))

- `showcases`：展示区块 ([查看配置示例](./区块配置示例/showcases/showcases.md))

- `showcases-flow`：瀑布流区块 ([查看配置示例](./区块配置示例/showcases/showcases-flow.md))

- `stats`：统计数据区块 ([查看配置示例](./区块配置示例/stats.md))

- `testimonials`：用户评价区块 ([查看配置示例](./区块配置示例/testimonials.md))

- `faq`：常见问题区块 ([查看配置示例](./区块配置示例/faq.md))

- `cta`：行动号召区块 ([查看配置示例](./区块配置示例/cta.md))

- `subscribe`：订阅区块 ([查看配置示例](./区块配置示例/subscribe.md))

- `footer`：页脚区块 ([查看配置示例](./区块配置示例/footer.md))

你可以在动态页面文件中，通过 `block` 字段指定要展示的区块，按照 `Section` 类型定义，传递区块展示的内容。


## 同一个区块复用多次（推荐写法）

如果你想在同一个页面里放两个同类型区块（例如两个 `features`），不要重复 key，而是让 key 唯一，然后用 `block` 指向同一个区块组件：

```json
{
  "page": {
    "sections": {
      "features_intro": {
        "block": "features",
        "title": "Intro",
        "items": []
      },
      "features_detail": {
        "block": "features",
        "title": "Details",
        "items": []
      }
    }
  }
}
```